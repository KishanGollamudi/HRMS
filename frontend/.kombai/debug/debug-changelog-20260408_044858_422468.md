# Debug Changelog — 2026-04-08

## Changes Made

### 1. Fixed undefined theme tokens in EmployeesPage
- **File:** `src/pages/EmployeesPage.jsx`
- **Change:** Replaced `G.accentDim` → `G.accentBg`, `G.card2` → `G.card`, `G.cardHover` → `G.bg2` — none of these keys exist in the manager theme (`manager.js`).
  Also fixed Add Employee button text color from `"#0a1628"` → `"#fff"` for contrast.
- **Revert:** Restore original `G.accentDim`, `G.card2`, `G.cardHover`, and `"#0a1628"` values.

### 2. Fixed nested setState calls in AttendanceContext
- **File:** `src/context/AttendanceContext.jsx`
- **Change:** In `updateStatus`, `markAllPresent`, and `submitAttendance`, removed calls to `getSession()` inside `setSessions()` updater callbacks. `getSession()` itself calls `setSessions`, so nesting them violates React rules. Replaced with a safe guard: if the session doesn't exist yet in `prev`, bail out early (return `prev`). Sessions are always initialized by `ensureSession` before these actions are triggered.
- **Revert:** Restore `|| getSession(sprintId, date)` fallback inside the setSessions updaters.

### 3. Fixed sprint ID type mismatch in SprintAttendance
- **File:** `src/features/sprint/pages/SprintAttendance.jsx`
- **Change:** Changed `sprints.find((s) => s.id === Number(id))` to `sprints.find((s) => String(s.id) === String(id))`. When HR creates a sprint in mock mode, `addSprint` assigns `id: crypto.randomUUID()` (a string). `Number(uuid)` returns `NaN`, so the sprint was never found via the URL param.
- **Revert:** Change back to `s.id === Number(id)`.

### 4. Fixed "DevOps" vs "Devops" case mismatch in SprintAttendance and Overview
- **Files:** `src/features/sprint/pages/SprintAttendance.jsx`, `src/features/sprint/pages/Overview.jsx`
- **Change:** Changed `"DevOps"` → `"Devops"` in `TECH_STACKS` arrays and `BAR_COLORS` key in both files. Employee data uses `technology: "Devops"` (lowercase 'ops'), so filtering by `"DevOps"` always returned 0 results.
- **Revert:** Change `"Devops"` back to `"DevOps"` in TECH_STACKS and BAR_COLORS.

### 5. Added ensureSession to useEffect dependency array
- **File:** `src/features/sprint/pages/SprintAttendance.jsx`
- **Change:** Added `ensureSession` to the `useEffect` dependency array. It was missing, which is a lint violation and could cause stale closure bugs.
- **Revert:** Remove `ensureSession` from the dep array.

### 6. Fixed avatar gradient hardcoded to teal for all roles in Header
- **File:** `src/components/Header.jsx`
- **Change:** Added `avatarGradient` per role in `ROLE_COLORS` (teal for trainer, rose for hr, dark for manager). Changed the avatar div's `background` style from the hardcoded teal gradient to `colors.avatarGradient`.
- **Revert:** Restore hardcoded `"linear-gradient(135deg,#0d9488,#14b8a6)"`.

### 7. Fixed breadcrumb navigation (full page reloads + active page semantics)
- **File:** `src/components/AppBreadcrumb.jsx`
- **Change:** 
  - Replaced `<BreadcrumbLink href="...">` with `<BreadcrumbLink asChild><Link to="...">` for client-side navigation (no full page reloads).
  - Replaced `<BreadcrumbLink>` (no href) for the active/current page with `<BreadcrumbPage>` for correct semantics and accessibility.
  - Fixed sprint ID lookup to use `String(s.id) === sprintMatch[1]` consistent with Bug 3.
- **Revert:** Restore `href` attributes, remove Link imports, revert BreadcrumbPage back to BreadcrumbLink.

## Revert Status
- [ ] Change 1 - EmployeesPage undefined theme tokens
- [ ] Change 2 - Nested setState in AttendanceContext
- [ ] Change 3 - Sprint ID type mismatch
- [ ] Change 4 - DevOps/Devops case mismatch
- [ ] Change 5 - ensureSession dependency array
- [ ] Change 6 - Avatar gradient per role
- [ ] Change 7 - Breadcrumb client-side navigation
